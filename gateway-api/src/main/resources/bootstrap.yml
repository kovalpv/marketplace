spring:
  application:
    name: gateway-api
  profiles:
    active:
      - ${CURRENT_ENV:default}
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: ${FAIL_FAST:true}
      retry:
        multiplier: 1.2
        max-attempts: 20
      enabled: true
